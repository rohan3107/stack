---
title: useStackApp
---

# useStackApp

A React hook that retrieves the current Stack app based on the URL parameter.

## Example

```tsx
import { StackServerApp } from '@stackframe/stack';

export const stackServerApp = new StackServerApp({
  tokenStore: "nextjs-cookie",
});
```