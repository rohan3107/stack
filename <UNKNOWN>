---
---
title: OAuthButton
---

# OAuthButton

Renders a button for OAuth authentication with various providers.

## Props

- `provider`: `string` - The name of the OAuth provider (e.g., 'google', 'github', 'facebook', etc.).
- `type`: `'sign-in' | 'sign-up'` - Determines whether the button is for signing in or signing up.

## Example

```tsx
import { OAuthButton } from '@stackframe/stack';

function AuthPage() {
  return (
    <div>
      <OAuthButton provider="google" type="sign-in" />
      <OAuthButton provider="github" type="sign-up" />
    </div>
  );
}
```
function LoginPage() {
  return (
    <AuthPage
      type="sign-in"
      fullPage={true}
      automaticRedirect={true}
      extraInfo={<p>By signing in, you agree to our Terms of Service.</p>}
    />
  );
}

function SignUpPage() {
  return (
    <AuthPage
      type="sign-up"
      firstTab="password"
      noPasswordRepeat={false}
    />
  );
}
```